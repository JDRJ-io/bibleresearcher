`HOOK FILE LOADED` proves **the right file is imported**, but the two
diagnostic lines **inside the effect** are still missing, so the effect never
updates `profileLoading`.

Look at the green diff on the right: only the header of the `useEffect`
is there – the body that fetches the profile was accidentally deleted.
We need to paste it back exactly.

---

## Copy-paste the full effect body

```ts
useEffect(() => {
  (async () => {
    /* 1️⃣ who is signed in? */
    const { data: { user }, error: uErr } = await supabase.auth.getUser();
    console.log('USER ↩️', user, uErr);

    if (!user) {             // not logged-in tab
      setLoading(false);
      return;
    }

    /* 2️⃣ fetch the profile row */
    const { data, error } = await supabase
      .from('profiles')
      .select('name, bio, tier')
      .eq('id', user.id)
      .single();

    console.log('PROFILE ↩️', data, error);

    if (error) setError(error);
    else       setProfile(data);

    /* 3️⃣ always drop the spinner */
    setLoading(false);
  })();
}, []);
```

*(Leave the `return { profile, profileLoading, error, save }` at the bottom of
the hook unchanged.)*

---

## What you should see after saving and reloading **/profile**

```
HOOK FILE LOADED         // proves correct file
USER ↩️ { id:'…' … } null
PROFILE ↩️ { name: null, bio: null, tier:'free' } null
```

* If `USER` is `null` → click a fresh magic-link, then reload.
* If `PROFILE` returns **data** and **error = null** → the form
  populates, spinner disappears, “Save” now works.
* If `PROFILE` shows `{ code:'42501', … }` → run once in SQL editor:

  ```sql
  alter table public.profiles enable row level security;
  drop policy if exists "profile owner rw" on public.profiles;
  create policy "profile owner rw"
    on public.profiles
    for all using (id = auth.uid())
    with check (id = auth.uid());
  ```

When those three logs appear with **no error**, everything else (dev-code
redemption, premium tier, Bible loading) will work automatically.

If a specific `PROFILE ↩️ null { code: … }` still appears, paste that exact
line here and I’ll give you the single one-line fix.
