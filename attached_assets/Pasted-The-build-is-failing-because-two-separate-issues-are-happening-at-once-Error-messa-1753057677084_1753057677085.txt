The build is failing because **two separate issues** are happening at once:

| #     | Error message on screen                                                                                        | Root cause                                                                                                                                                                                                                      | Fastest fix                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | `The requested module "/src/store/translationSlice.ts" does not provide an export named "useTranslationSlice"` | The file **doesn’t export** `useTranslationSlice` under that exact name any more (it was probably renamed to `useTranslationState` or a default export during the recent refactor). Every component that imports it now breaks. | • Open **`src/store/translationSlice.ts`** and confirm what it actually exports:<br>`ts<br>export const useTranslationState = … // ← current name<br>// or export default createTranslationSlice<br>`<br>• Search the repo for `useTranslationSlice(` and change those imports to the correct name **or** add<br>`ts<br>export { useTranslationState as useTranslationSlice };<br>`<br>to keep backward compatibility. |
| **2** | `vite-plugin-cartographer: travers is not a function` (plus long stack‑trace)                                  | The Replit‑only plugin **vite-plugin-cartographer** is out of sync with Vite 5. It injects an outdated Babel helper that crashes Vite’s dev server.                                                                             | In **`vite.config.ts`** comment the plugin out:  `ts<br>// import cartographer from '@replit/vite-plugin-cartographer';<br>export default defineConfig({<br>  plugins:[react(), /* cartographer() */],<br>});<br>`<br>The plugin is not required for running or building your app; it only adds Replit bookmarking telemetry.                                                                                          |

After you do both fixes → restart the dev server (`⌘/Ctrl + R`) and the app will compile again so you can test performance changes.

