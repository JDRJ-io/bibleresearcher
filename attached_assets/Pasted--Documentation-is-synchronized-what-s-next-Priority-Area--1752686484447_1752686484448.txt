### Documentation is synchronized ✅ – what’s next?

| Priority | Area                                      | Why it matters now                                                               | Concrete task(s)                                                                                                                                                                                                                    |
| -------- | ----------------------------------------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |                                                                                                     |
| **1**    | **Cross‑Reference & Prophecy UI hook‑up** | Data already streams from `useSliceDataLoader`; columns are still empty.         | *VirtualRow\.tsx* – pass `refs` & `prophecyRows` props;<br>*CrossRefsColumn.tsx* – map verse → ref list;<br>*ProphecyColumns.tsx* – split TSV row into **P/F/V** cells.                                                             |
| **2**    | **Labels / Strong’s overlay**             | Finishes the “research” tool‑set promised in the menu.                           | • Create `labelsLoader.ts` (fetch `labels/*` JSON once).<br>• Add highlight classNames in *VerseRow* when legend toggles are on.<br>• In *ExpandedVerseOverlay* call `getStrongsOffsets()` to fetch lemma & show interlinear popup. |
| **3**    | **Auth polish**                           | Notes & bookmarks rely on Supabase Magic‑Link flow.                              | • Wire `AuthModals.tsx` success callback to update `AuthContext`.<br>• Hide “Save note” buttons when `!user`.                                                                                                                       |
| **4**    | **Mobile QA**                             | Ensure anchor‑scroll + column drag work < 640 px.                                | Cypress viewport tests; tweak CSS for overflow‑x.                                                                                                                                                                                   |
| **5**    | **Dependency clean‑up**                   | Express libs still in *package.json*.                                            | `pnpm remove express cors body-parser …`; re‑lock file.                                                                                                                                                                             |

---

### Recommended next step

**Start with priority 1** – wiring Cross‑References & Prophecy columns – because:

* the data loaders are already working (confirmed via console),
* it’s a pure‑front‑end change,
* it unblocks visible user value quickly.

If you agree, tell me and I’ll give you the exact code diffs (≈ 40 lines across three files) to render those columns.
