import { useColumnKeys } from '@/store/translationSlice';

/**
 * COLUMN KEYS DERIVED SELECTOR
 * 
 * Returns memoized column keys based on translation state
 * Format: [...alternates, main] - as specified by expert
 */
export { useColumnKeys };